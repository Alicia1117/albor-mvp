{
    "name": "albor-mvp",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
        "frontend",
        "backend",
        "shared"
    ],
    "scripts": {
        "dev": "concurrently -n SHARED,BACKEND,FRONTEND -c cyan,magenta,green \"npm:dev -w @albor-mvp/shared\" \"npm:dev -w backend\" \"npm:dev -w frontend\"",
        "build": "npm run build -w @albor-mvp/shared && npm run build -w backend && npm run build -w frontend",
        "clean": "rimraf node_modules package-lock.json && npm run clean -w @albor-mvp/shared || exit 0 && npm run clean -w backend || exit 0 && npm run clean -w frontend || exit 0"
    },
    "devDependencies": {
        "concurrently": "^9.0.0",
        "rimraf": "^5.0.10"
    },
    "dependencies": {
        "zod": "^4.0.17"
    }
}
